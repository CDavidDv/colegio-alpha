---
import CardFilosofia from "./CardFilosofia.astro";
// Componente Filosofía institucional para Colegio Alpha
---
<section id="filosofia" class="py-16">
  <div class="max-w-6xl mx-auto px-4 text-center">
    <h2 class="text-3xl md:text-4xl font-bold text-[var(--steam-azul-oscuro)] mb-6">¿Por qué elegirnos?</h2>
    <p class="text-lg text-gray-700 mb-8">En Colegio Alpha entendemos que la educación de tus hijos es la inversión más importante. Por eso, nos comprometemos a ofrecer un ambiente seguro, cálido y estimulante donde cada niño pueda desarrollar su potencial único.</p>
    <div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-8">
      
      <CardFilosofia
        img="/deportes.webp"
        emoji="⚽"
        titulo="Deportes"
        texto="Fomentan el trabajo en equipo, la disciplina y el desarrollo físico saludable en los niños."
        colorBorde="#67BC29"
        colorTitulo="#67BC29"
        svgPath="M0 30 Q200 90 400 30 V60 H0Z"
        alt="Deportes"
      />
      <CardFilosofia
        img="/alpha- (9).webp"
        emoji="🎉"
        titulo="Eventos y Campamentos" 
        texto="Desarrollamos habilidades sociales, liderazgo y creatividad a través de experiencias divertidas y seguras."
        colorBorde="#FF7300"
        colorTitulo="#FF7300"
        svgPath="M0 40 Q200 70 400 40 V60 H0Z"
        alt="Eventos y Campamentos"
      />
      <CardFilosofia
        img="/eeuu_flag.webp" 
        emoji="🌎"
        titulo="Inglés y Francés"
        texto="Facilitamos la comunicación global y enriquecemos el aprendizaje cultural a través del inglés y francés, abriendo puertas a nuevas oportunidades."
        colorBorde="#E4E41A"
        colorTitulo="#E4E41A"
        svgPath="M0 40 Q200 70 400 40 V60 H0Z"
        alt="Idiomas"
      />
      <CardFilosofia
        img="/artes.webp"
        emoji="🎨"
        titulo="Artes y Cultura"
        texto="Desarrollamos la creatividad, expresión artística y apreciación cultural en nuestros estudiantes."
        colorBorde="#2196F3"
        colorTitulo="#2196F3"
        svgPath="M0 40 Q200 70 400 40 V60 H0Z"
        alt="Artes y Cultura"
      />
    </div>
  </div>
</section>

<script>
// Efecto de seguimiento al mouse en las cards
if (typeof window !== 'undefined') {
  setTimeout(() => {
    const cards = document.querySelectorAll('.filo-card-move');
    cards.forEach(card => {
      card.addEventListener('mousemove', (e) => {
        const event = e as MouseEvent;
        const el = card as HTMLElement;
        const rect = el.getBoundingClientRect();
        const x = event.clientX - rect.left;
        const y = event.clientY - rect.top;
        const moveX = ((x / rect.width) - 0.5) * 16; // máx ±8px
        const moveY = ((y / rect.height) - 0.5) * 16;
        el.style.transform = `translate(${moveX}px, ${moveY}px) scale(1.03)`;
        el.style.transition = 'transform 0.15s';
      });
      card.addEventListener('mouseleave', () => {
        const el = card as HTMLElement;
        el.style.transform = 'translate(0,0) scale(1)';
        el.style.transition = 'transform 0.35s cubic-bezier(.22,1,.36,1)';
      });
    });
  }, 300);
}
</script>

<style>
.filo-card-move {
  will-change: transform;
}
</style> 